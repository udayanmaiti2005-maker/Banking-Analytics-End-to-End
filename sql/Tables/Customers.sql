CREATE TABLE IF NOT EXISTS Customers (
    Customer_ID INT PRIMARY KEY,
    NAME VARCHAR(100),
    Age INT,
    Gender VARCHAR(10),
    City VARCHAR(50),
    Occupation VARCHAR(50),
    Annual_Income INT,
    Credit_Score INT
);